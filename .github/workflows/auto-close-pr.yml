name: Auto Close External PRs

on:
  pull_request_target:
    types: [opened, reopened]

jobs:
  close:
    runs-on: ubuntu-latest
    steps:
      - name: Close external PR
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            const allowed = ['Aidank-cy', 'github-actions[bot]'];

            if (allowed.includes(pr.user.login)) {
              core.info(`PR by ${pr.user.login} is allowed, skipping.`);
              return;
            }

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              body: [
                'This repository is automatically synced from a private dev repo.',
                '',
                'Direct pull requests are not accepted here.',
                'If you have suggestions or found a bug, please open an issue instead.',
                '',
                'Thank you for your interest.'
              ].join('\n')
            });

            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
              state: 'closed'
            });

            core.info(`Closed PR #${pr.number} by ${pr.user.login}`);
